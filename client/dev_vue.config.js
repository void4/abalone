const fs = require('fs');

module.exports = {
    devServer: {
        /*
        disableHostCheck: true,
        https: true,
        host: '0.0.0.0',
        port: 443,       // ssl defult port number
        inline: true,
        https: {
              key: fs.readFileSync('/etc/letsencrypt/live/qewasd.com/privkey.pem'),
              cert: fs.readFileSync('/etc/letsencrypt/live/qewasd.com/cert.pem'),
              ca: fs.readFileSync('/etc/letsencrypt/live/qewasd.com/chain.pem')
        }
        */
    }
}
